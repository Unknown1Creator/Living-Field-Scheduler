Living Field Scheduler:


1. System Pressure Field

The core system state is represented by the pressure vector:
P=[PcpuPmemPioPnetPfs]?R5\mathbf{P} = \begin{bmatrix} P_{\text{cpu}} \\ P_{\text{mem}} \\ P_{\text{io}} \\ P_{\text{net}} \\ P_{\text{fs}} \end{bmatrix} \in \mathbb{R}^5\mathbf{P} = \begin{bmatrix} 
P_{\text{cpu}} \\ 
P_{\text{mem}} \\ 
P_{\text{io}} \\ 
P_{\text{net}} \\ 
P_{\text{fs}}
\end{bmatrix} \in \mathbb{R}^5
with domain constraints:
P?clamp(P, 0.0, 9.5)\mathbf{P} \leftarrow \text{clamp}(\mathbf{P}, \, 0.0, \, 9.5)\mathbf{P} \leftarrow \text{clamp}(\mathbf{P}, \, 0.0, \, 9.5)
2. Global Pressure ScoreThe scalar system pressure is computed as the weighted sum:
Sglobal=wTP=?i=15wiPiS_{\text{global}} = \mathbf{w}^T \mathbf{P} = \sum_{i=1}^{5} w_i P_iS_{\text{global}} = \mathbf{w}^T \mathbf{P} = \sum_{i=1}^{5} w_i P_i
where the fixed weight vector is:
w=[1.40, 1.15, 1.05, 0.85, 0.75]\mathbf{w} = [1.40, \, 1.15, \, 1.05, \, 0.85, \, 0.75]\mathbf{w} = [1.40, \, 1.15, \, 1.05, \, 0.85, \, 0.75]
3. Holistic Compression (Equilibrium Pull)Applied every tick after propagation and scheduling:
?=15?i=15Pi\mu = \frac{1}{5} \sum_{i=1}^{5} P_i\mu = \frac{1}{5} \sum_{i=1}^{5} P_i
P?0.72?P+0.28??\mathbf{P} \leftarrow 0.72 \cdot \mathbf{P} + 0.28 \cdot \mu\mathbf{P} \leftarrow 0.72 \cdot \mathbf{P} + 0.28 \cdot \mu
4. Non-Local PropagationUpon any discrete event with source delta 
?\delta\delta
(positive for load, negative for relief):
Psource?Psource+?P_{\text{source}} \leftarrow P_{\text{source}} + \deltaP_{\text{source}} \leftarrow P_{\text{source}} + \delta
Pj?Pj+????j?sourceP_j \leftarrow P_j + \delta \cdot \kappa \quad \forall j \neq \text{source}P_j \leftarrow P_j + \delta \cdot \kappa \quad \forall j \neq \text{source}
where the diffusion coefficient 
??0.28\kappa \approx 0.28\kappa \approx 0.28
governs gentle non-local coupling.5. Task Demand and State ModelEach task ( t ) maintains a normalized demand vector:
Dt?R5,?Dt?1=1,Dt,i?0\mathbf{D}_t \in \mathbb{R}^5, \quad \|\mathbf{D}_t\|_1 = 1, \quad D_{t,i} \geq 0\mathbf{D}_t \in \mathbb{R}^5, \quad \|\mathbf{D}_t\|_1 = 1, \quad D_{t,i} \geq 0
along with progress and age:
progresst?[0,1],aget?N0\text{progress}_t \in [0, 1], \quad \text{age}_t \in \mathbb{N}_0\text{progress}_t \in [0, 1], \quad \text{age}_t \in \mathbb{N}_0
6. Task Scheduling ScoreThe holistic scheduling score for a ready task ( t ) is defined as:
score(t)=? k?Dt?P ?2+c?(1?progresst)???aget\text{score}(t) = \|\, k \cdot \mathbf{D}_t - \mathbf{P} \,\|_2 + c \cdot (1 - \text{progress}_t) - \beta \cdot \text{age}_t\text{score}(t) = \|\, k \cdot \mathbf{D}_t - \mathbf{P} \,\|_2 + c \cdot (1 - \text{progress}_t) - \beta \cdot \text{age}_t
where:
    • k?4.2k \approx 4.2k \approx 4.2
      (demand scaling factor)
    • c>0c > 0c > 0
      (completion urgency coefficient)
    • ?>0\beta > 0\beta > 0
      (aging boost coefficient for starvation prevention)
The scheduler selects up to 
NcoresN_{\text{cores}}N_{\text{cores}}
ready tasks with the lowest scores.7. Task Progress UpdateDuring execution, progress advances as:
?progresst=rbase?f(Sglobal,Dt)\Delta \text{progress}_t = r_{\text{base}} \cdot f(S_{\text{global}}, \mathbf{D}_t)\Delta \text{progress}_t = r_{\text{base}} \cdot f(S_{\text{global}}, \mathbf{D}_t)
where the pressure-and-demand modulation function ( f ) is:
f(Sglobal,Dt)=max?(0.35, 2.4?0.16?Sglobal)?(1+??cos??(Dt,P))f(S_{\text{global}}, \mathbf{D}_t) = \max\left(0.35, \, 2.4 - 0.16 \cdot S_{\text{global}}\right) \cdot \left(1 + \gamma \cdot \cos\theta(\mathbf{D}_t, \mathbf{P})\right)f(S_{\text{global}}, \mathbf{D}_t) = \max\left(0.35, \, 2.4 - 0.16 \cdot S_{\text{global}}\right) \cdot \left(1 + \gamma \cdot \cos\theta(\mathbf{D}_t, \mathbf{P})\right)
(with 
cos??\cos\theta\cos\theta
representing directional alignment between demand and current pressure; 
?\gamma\gamma
is a small alignment bonus).8. Completion Relief PropagationUpon task completion (
progresst?1\text{progress}_t \geq 1\text{progress}_t \geq 1
):
?relief=?1.15??Dt?1(or stronger weighted variant)\delta_{\text{relief}} = -1.15 \cdot \|\mathbf{D}_t\|_1 \quad (\text{or stronger weighted variant})\delta_{\text{relief}} = -1.15 \cdot \|\mathbf{D}_t\|_1 \quad (\text{or stronger weighted variant})
Then apply the non-local propagation rule (Section 4) with 
?=?relief\delta = \delta_{\text{relief}}\delta = \delta_{\text{relief}}
.9. Critical State DampingIf 
Sglobal>22.0S_{\text{global}} > 22.0S_{\text{global}} > 22.0
for consecutive epochs, apply emergency contraction:
P?0.82?P\mathbf{P} \leftarrow 0.82 \cdot \mathbf{P}\mathbf{P} \leftarrow 0.82 \cdot \mathbf{P}
followed by strengthened compression (
?strong=0.65\alpha_{\text{strong}} = 0.65\alpha_{\text{strong}} = 0.65
).10. Observability MetricsKey monitored quantities include:
    • Sglobal(t)S_{\text{global}}(t)S_{\text{global}}(t)
    • ?P(t)?2\|\mathbf{P}(t)\|_2\|\mathbf{P}(t)\|_2
      and component-wise values
    • Mean pressure 
      ?(t)\mu(t)\mu(t)
    • Cumulative completed tasks
    • Distribution of task scores and aging statistics


